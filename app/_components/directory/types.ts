export enum DirectorySortMode {
  Count = "count",
  Recent = "recent",
  Name = "name",
}

export enum DirectoryEmptyReason {
  Unavailable = "unavailable",
  Source = "source",
  Query = "query",
  Geography = "geography",
  Combined = "combined",
}
